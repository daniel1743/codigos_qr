# POWER EDITOR PHASE 4 — QA SELECTION FIXTURE V1

## Summary

Phase 4A code remains frozen and unchanged. This task only produced a valid,
deterministic DEV/QA document (plus its minimal DEV-only loading seam) so the
manual SELECT-01..08 gate can be executed against trustworthy, vertically
distributed content.

The fixture loads without touching Supabase, canonical persistence, autosave,
or any production storage, and is unreachable in production builds.

FILES_READ:
- src/premium-template-studio/types/index.ts (BioTemplateConfig, BlockType, TemplateBlock, BlockLayout, BlockContent)
- src/premium-template-studio/engine/BlockRegistry.ts (registered block types)
- src/premium-template-studio/engine/TemplateBuilder.ts (buildTemplate)
- src/premium-template-studio/constants/blockDefinitions.ts (block defaults / content shapes)
- src/premium-template-studio/constants/layouts.ts (LayoutId / getLayout)
- src/premium-template-studio/templates/definitions.ts (createDemoConfig / creatorPremium reference)
- src/premium-template-studio/components/PremiumTemplateStudio.tsx (props + self-contained shell)
- src/premium-template-studio/index.ts (public exports)
- src/components/power-editor/PowerEditorHost.tsx (existing loading path — reference only)
- src/routes/power-editor.tsx, internal.power-editor.tsx, engine-lab.tsx, onboarding-preview.tsx (route conventions)
- src/routes/__root.tsx, src/router.tsx, src/routeTree.gen.ts (routing)

FILES_MODIFIED:
- src/premium-template-studio/fixtures/powerEditorPhase4SelectionQaFixture.ts (NEW)
- src/premium-template-studio/fixtures/__tests__/powerEditorPhase4SelectionQaFixture.test.ts (NEW)
- src/routes/power-editor-phase4-qa.tsx (NEW)
- src/routeTree.gen.ts (auto-regenerated by the TanStack Router Vite plugin — generated output, not hand-edited)

CANONICAL_TYPE_USED: BioTemplateConfig (built via `buildTemplate`)
SUPPORTED_BLOCK_TYPES_USED: hero, heading, text, image, cta, buttonGroup, contact
NUMBER_OF_BLOCKS: 10
VERTICAL_DISTRIBUTION:
  - TOP: hero (`qa-phase4-top`) + heading + text
  - UPPER-MID: image (video aspect) + cta + buttonGroup
  - MIDDLE: heading (`qa-phase4-middle`) + text
  - LOWER: image (video aspect)
  - BOTTOM: contact (`qa-phase4-bottom`)

FIXTURE_LOCATION: src/premium-template-studio/fixtures/powerEditorPhase4SelectionQaFixture.ts
FIXTURE_DEV_ONLY: YES (only referenced by a `import.meta.env.DEV`-guarded route)
PRODUCTION_EXPOSURE: NONE (route throws `notFound()` outside DEV; not linked in navigation)
DATABASE_WRITES: NONE
CANONICAL_PERSISTENCE_WRITES: NONE (autoSave disabled; no storage adapter invoked)
ENGINE_V2_USED: NO

LOADING_MECHANISM: new DEV-only route `/power-editor-phase4-qa` mounting `<PremiumTemplateStudio config={fixture} autoSave={false} />`
EXISTING_MECHANISM_REUSED: NO (none existed — PowerEditorHost requires auth + persisted Supabase profile)

PHASE4_FILES_UNCHANGED: YES (PowerCanvasViewport.tsx, powerCanvasAutofocus.ts untouched)
CAMERA_UNCHANGED: YES
PERSISTENCE_UNCHANGED: YES
HISTORY_UNCHANGED: YES

TESTS: PASS
  - powerCanvasCameraMath: 17/17 (unchanged, green)
  - powerCanvasAutofocus: 6/6 (unchanged, green)
  - powerEditorPhase4SelectionQaFixture: 5/5 (new — validity, block count, unique ids, QA ids present, visibility)

HTTP_EDITOR_SMOKE: PASS — `GET /editor` returned 200 (no 500) on the running dev server (localhost:8080)
RUNTIME_FIXTURE_RENDER: PASS — `GET /power-editor-phase4-qa` returned 200; SSR output contained `data-block-id` for all 10 QA ids and the QA title text
BUILD: NOT_VERIFIED (full `vite build` / `tsc --noEmit` not completed; SSR render via the dev server is the runtime evidence)

READY_FOR_SELECT_01_08: YES
STOP_CONDITION_TRIGGERED: NO
SCOPE_EXPANSION_REQUIRED: NO

POWER_EDITOR_PHASE4_QA_SELECTION_FIXTURE_GATE:
PASS
